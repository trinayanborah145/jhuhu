import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: `${process.env.COMPANY_NAME || 'Sukrit Infras'} | Premium Construction & Renovations`,
  description: `Professional architectural builds, custom renovations, and home extensions by ${process.env.COMPANY_NAME || 'Sukrit Infras'} serving ${process.env.COMPANY_SERVICE_AREA || 'London and surrounding home counties'}.`,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
